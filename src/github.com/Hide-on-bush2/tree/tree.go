package tree

type node struct {
	val   int
	left  *node
	right *node
}

func make_empty(num int) {
	return node{val: num, left: nil, right: nil}
}

func insert(num int, tree *node) {
	if node == nil {
		return make_empty(num)
	}

	if node.val > num {

	}
}
